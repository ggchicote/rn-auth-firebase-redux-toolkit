import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // content: { flex: 1 },
  container: { gap: 5 },
  label: { fontSize: 13 },
  error: { fontSize: 11, color: 'red' },
  input: {
    borderColor: '#000',
    borderBottomWidth: 1,
    paddingVertical: 10,
  },
});
